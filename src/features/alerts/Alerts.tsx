import React, { useEffect } from 'react';
import {
  useSelector,
  useDispatch
} from 'react-redux';
import { } from '@material-ui/core';
import { Alert } from '@material-ui/lab';

interface AlertsProps {
  maxAlerts?: number;
}

export const Alerts: React.FunctionComponent<AlertsProps> = () => {
  React.useEffect(() => {
  });

  return (
    <p>test</p>
  );
};
